# Vite env for the Tauri desktop build (loaded via `vite build --mode tauri`).
#
# The desktop backend origin and one-time token are negotiated with the Rust
# shell at runtime. No fixed port or reusable credential is baked into assets.
# Regular web/dev builds continue to use same-origin requests and Vite's proxy.
